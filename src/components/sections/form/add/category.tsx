import { CategoryType } from '@/types';

const Category = ({
  className,
  type
}: {
  className?: string;
  type: CategoryType;
}) => {
  return <div>Add Category</div>;
};

export default Category;
